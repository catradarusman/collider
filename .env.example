# Copy to .env and fill in.
ANTHROPIC_API_KEY=sk-ant-...
# Optional overrides:
# PORT=3000
# MODEL=claude-haiku-4-5-20251001
# ALLOWED_ORIGINS=https://collider.catra.fyi   # only if the frontend is hosted separately; not needed for the single-service setup
# Analytics (optional): logs categorize/collide/ideas/feedback to a Supabase table.
# Leave unset to disable logging entirely.
# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_SERVICE_KEY=eyJ...   # service_role key, server-side only. Never expose to the frontend.
